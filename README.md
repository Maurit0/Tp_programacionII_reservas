# Simulador de Reservas - Arena Rosario (Pádel & Fútbol Center)

Trabajo Práctico N° 1 para la asignatura **Programación II**  
**Tecnicatura Universitaria en Programación - UTN FRRo**

## Integrantes
* **Mauro Alegre** (Maurit0)
* **Tomás Ayala** (ayalatomas-tsa)

## Descripción del Proyecto
Aplicación web frontend para la gestión y reserva de turnos en un complejo deportivo (canchas de pádel y fútbol 5 sintético). Permite a los usuarios consultar las instalaciones, reservar un turno mediante un formulario con validaciones en tiempo real, y administrar sus reservas (listar, editar y cancelar) simulando persistencia de datos mediante `localStorage`.

## Tecnologías Utilizadas
* **HTML5:** Estructura semántica completa (`<header>`, `<nav>`, `<main>`, `<article>`, `<section>`, `<footer>`).
* **CSS3:** Maquetación moderna utilizando **Flexbox** y **CSS Grid** de forma nativa (sin frameworks tipo Bootstrap ni Tailwind).
* **JavaScript (ES6+):** Manipulación dinámica del DOM, validación con expresiones regulares, eventos y almacenamiento con la API `localStorage`.
* **Control de versiones:** Git y GitHub.

## Cómo ejecutar el proyecto
1. Clonar el repositorio:
   ```bash
   git clone <URL_DEL_REPOSITORIO>
   ```
2. Abrir la carpeta en Visual Studio Code.
3. Hacer clic derecho en `index.html` y seleccionar **"Open with Live Server"** (o abrir el archivo directamente en cualquier navegador web moderno).
